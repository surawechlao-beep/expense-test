(()=>{let rows=[];try{rows=JSON.parse(sessionStorage.getItem('exion_perf:'+CONFIG.API_URL)||'[]');}catch(e){}if(!Array.isArray(rows))rows=[];const tbody=document.getElementById('samples');rows.slice().reverse().forEach(r=>{const tr=document.createElement('tr');[r.at||'',r.action||'',(r.ms/1000).toFixed(2),r.serverMs==null?'—':(r.serverMs/1000).toFixed(2)].forEach(value=>{const td=document.createElement('td');td.textContent=value;tr.append(td);});tbody.append(tr);});if(!rows.length)document.getElementById('empty').textContent='ยังไม่มีข้อมูล เปิดใช้งานเว็บแล้วกลับมาหน้านี้ในแท็บเดิม';document.getElementById('download').onclick=()=>{const url=URL.createObjectURL(new Blob([JSON.stringify({build:'8.0.6',samples:rows},null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download='exion-load-times.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);};})();